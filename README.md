# GlintX
GlintX is an phone cleaning station that performs a hands-free cleaning cycle using a motorized brush mechanism. The system uses microfiber rollers, motors, and Arduino software to clean the screen and remove fingerprints, dust, and surface contaminants with minimal user interaction. This creates a compact device that automates a routine task .
